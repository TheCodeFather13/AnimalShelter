@page "/animals"

<h3>Animals</h3>
@inject NavigationManager NavigationManager

<button class="btn btn-primary" @onclick="OnCancel">Back</button>
@code {
    private void OnCancel()
    {
        NavigationManager.NavigateTo("/admin");
    }
}
